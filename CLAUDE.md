# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is an issue tracker application with a multi-tier architecture:

- `backend/` — Server-side API/service layer
- `client/` — Frontend UI
- `infrastructure/` — Deployment and DevOps configuration

## Commands

_To be filled in as the project is scaffolded (build, lint, test commands)._
