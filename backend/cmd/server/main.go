// Package main is the composition root for the issue-tracker server.
// It wires all layers together and starts the Echo HTTP server.
package main

import "fmt"

func main() {
	fmt.Println("issue-tracker server starting")
}
